import { Card } from '@/components/ui/Card'
import { formatPercent } from '@/utils/formatters'

export function RecentPredictionsTable({ rows = [] }) {
  return <Card title="Recent predictions" subtitle="Latest scored customers from the live prediction history"><div className="overflow-x-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="border-b border-slate-200 text-xs uppercase tracking-wider text-slate-500 dark:border-slate-700"><tr><th className="pb-3 font-medium">Customer ID</th><th className="pb-3 font-medium">Prediction</th><th className="pb-3 font-medium">Risk</th><th className="pb-3 font-medium">Probability</th><th className="pb-3 font-medium">Confidence</th><th className="pb-3 font-medium">Time</th></tr></thead><tbody>{rows.length ? rows.slice(0, 6).map((row) => { const risk = String(row.risk_level || 'Unknown').replace(/[^a-z ]/gi, '').trim(); return <tr key={row.id || `${row.customer_id}-${row.timestamp}`} className="border-b border-slate-100 last:border-0 dark:border-slate-800"><td className="py-3 font-medium text-slate-900 dark:text-white">{row.customer_id || '—'}</td><td className="py-3">{Number(row.prediction) === 1 ? 'Likely churn' : 'Retain'}</td><td className="py-3"><span className={`rounded-full px-2 py-1 text-xs font-semibold ${risk.toLowerCase().includes('high') ? 'bg-rose-100 text-rose-700' : 'bg-emerald-100 text-emerald-700'}`}>{risk || 'Unknown'}</span></td><td className="py-3">{formatPercent(Number(row.probability || 0))}</td><td className="py-3">{row.confidence_score == null ? 'Not Available' : formatPercent(Number(row.confidence_score))}</td><td className="py-3 text-slate-500">{row.timestamp ? new Date(row.timestamp).toLocaleString() : '—'}</td></tr> }) : <tr><td colSpan="6" className="py-8 text-center text-slate-500">No prediction history is available yet.</td></tr>}</tbody></table></div></Card>
}
